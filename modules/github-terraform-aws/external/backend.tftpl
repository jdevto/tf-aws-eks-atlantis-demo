terraform {
  backend "s3" {
    bucket       = "${state_bucket_name}"
    key          = "${repository_name}/terraform.tfstate"
    region       = "${region}"
    use_lockfile = true
    encrypt      = true
  }
}
